# 今日时间线 · 使用说明

一个为你定制的 macOS 菜单栏应用：读取系统「日历」中的日程，用时间线展示今日安排，并实时显示进行中日程的倒计时。

## 安装

应用已安装到「应用程序」文件夹（`今日时间线.app`），从启动台或 Finder 双击即可启动。

## 首次使用

1. 启动后屏幕会弹出「允许访问日历」的授权框 → 点击 **允许**。
   - 如果之前拒绝了，可打开 系统设置 → 隐私与安全性 → 日历，勾选「今日时间线」。
2. 授权后，顶部菜单栏（右上角）会出现一个日历图标，旁边显示实时倒计时：
   - 有日程进行中 → 显示该日程的剩余时间（如 `24:31`）
   - 空闲时 → 显示距离下一项的时间（如 `-15:20`）
3. 点击菜单栏图标，打开下拉面板：顶部是「进行中 / 下一项」状态卡，下方是今日时间线。

## 功能一览

| 功能 | 说明 |
|---|---|
| 今日时间线 | 按时间排列今天的日程，进行中的日程会高亮并标注「进行中」，已结束的会变灰 |
| 实时倒计时 | 菜单栏、面板、浮窗三处每秒刷新 |
| 下一项预告 | 空闲时自动显示下一项日程及开始倒计时 |
| 桌面浮窗 | 设置 → 桌面浮窗 开启后，屏幕右上角出现半透明小卡片，可拖动位置，点 ✕ 关闭 |
| 开始前提醒 | 日程开始前 5/10/15/30 分钟发系统通知（设置里可选，默认提前 10 分钟） |
| 日历筛选 | 设置 → 日历筛选，可勾选要显示的日历（iCloud、Google 等账户分开列出，颜色与日历 App 一致） |
| 跳转日历 | 点击时间线中的任意日程，直接在系统「日历」App 中定位到它 |
| 开机自启 | 设置 → 通用 → 登录时自动启动（首次开启需在 系统设置 → 通用 → 登录项 中确认） |

## 菜单栏显示模式

设置 → 菜单栏显示：仅图标 / 仅倒计时 / 图标 + 倒计时 / 图标 + 名称 + 倒计时（默认）。
「图标 + 名称 + 倒计时」会同时显示图标、当前进行中日程的完整名称和剩余时间（倒计时始终完整显示，空间不足时名称才会省略）；空闲时显示下一项名称与开始倒计时。
设置 → 菜单栏显示 → 倒计时颜色：浅色（默认）/ 深色 / 日历颜色（跟随日程所属日历的颜色，并随浅深色主题自动适配）。

## Touch Bar（常驻）

- 采用与歌词应用 **LyricsX 相同的系统级实现**，日程信息**常驻 Touch Bar**，切换任何应用都不会消失。
- 显示为两张**卡片**：
  - 「当前日程」卡：标签 + 日历颜色点 + 名称 + **进度条** + 倒计时；
  - 「下一项日程」卡：标签 + 颜色点 + 名称 + **开始时间（几点钟）**。
- **点击任意卡片**会在桌面打开日程浮窗。
- **最左侧的 `esc` 按钮**提供 Esc 键功能（首次点击会请求「辅助功能」权限，允许后即发送真实 Esc 键事件）。
- 设置 → Touch Bar：总开关、进行中日程、下一项日程、**卡片居中/靠左**。
- **Control Strip（右侧系统栏）** 中有本应用的图标按钮：点一下收起信息，再点一下重新显示。
- 注意：常驻会**替换**其他应用的 Touch Bar 内容（与 LyricsX 一致）；此实现使用苹果私有接口，若未来 macOS 更新移除了该接口，应用会自动降级、不影响其他功能。

## 常见问题

- **菜单栏里看不到图标？** 图标很多时会自动隐藏。按住 Command 键拖动菜单栏图标可调整顺序；本应用菜单栏图标为「日历+时间线」样式。
- **应用图标风格？** 当前为奶油底色 + 墨色时间轴 + 陶土橙「当前时刻」圆点（Claude 风格）；深空黑备选版见 `TodayTimeline/图标预览/`，想要切换直接告诉 DeepSeek 助手。
- **日历里新加的日程没显示？** 应用每 60 秒自动刷新，在日历 App 中改动后会立刻刷新；也可以点面板右上角的 ⟳ 手动刷新。
- **提醒通知没收到？** 检查 系统设置 → 通知 → 今日时间线 是否允许通知。
- **想改颜色/布局/加功能？** 直接告诉 DeepSeek 助手即可，改完会重新编译并替换应用。

## 更新与卸载

- 更新：由 DeepSeek 助手重新编译并安装，日历授权通常无需重复操作（若弹出授权框，再允许一次即可）。
- 卸载：在应用面板的 设置 → 退出应用；然后将「应用程序」中的 `今日时间线.app` 移到废纸篓。
